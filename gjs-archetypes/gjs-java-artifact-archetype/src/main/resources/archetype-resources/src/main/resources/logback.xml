#set( $symbol_pound = '#' )
#set( $symbol_dollar = '$' )
#set( $symbol_escape = '\' )
<configuration>
	<property name="LOG_DIR" value="@log.path@" />
	<property name="LOG_ARCHIVED_DIR" value="@log.archived.path@" />
	
	<appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
		<file>${symbol_dollar}{LOG_DIR}/${shortName}.log</file>

		<rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
			<fileNamePattern>${symbol_dollar}{LOG_ARCHIVED_DIR}/archived/${shortName}.%d{yyyy-MM-dd}.%i.log.gz</fileNamePattern>
			
			<maxFileSize>10MB</maxFileSize>
			
			<maxHistory>30</maxHistory>
			<totalSizeCap>3GB</totalSizeCap>
		</rollingPolicy>

		<encoder>
			<pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
		</encoder>
	</appender>
	<appender name="STDOUT" class="ch.qos.logback.core.ConsoleAppender">
		<encoder>
			<pattern>%d{HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
		</encoder>
	</appender>

    <logger name="${package}" level="INFO" additivity="false">
        <appender-ref ref="ASYNC" />
    </logger>

    <logger name="org.gjs.java.common" level="INFO" additivity="false">
        <appender-ref ref="ASYNC" />
    </logger>

	<root level="INFO">
		<appender-ref ref="STDOUT" />
	</root>
</configuration>