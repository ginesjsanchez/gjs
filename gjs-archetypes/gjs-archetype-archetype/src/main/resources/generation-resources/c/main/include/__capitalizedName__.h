#set($tagLib = _${package.replaceAll(".","_").toUpperCase()}_${shortName.toUpperCase()}_H)
#ifndef ${tagLib}
#define ${tagLib}

// Library includes
#include <Sample.h>
#include <SampleType.h>


#endif
